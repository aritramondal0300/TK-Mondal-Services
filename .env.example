# Fallback Neon/Vercel Postgres Database URL (if not using Cloudflare D1)
# Copy this file to .env.local and replace with your database credentials.
DATABASE_URL="postgresql://user:password@hostname:5432/dbname?sslmode=require"
