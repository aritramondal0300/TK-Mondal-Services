/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',

  basePath: '/TK-Mondal-Services',

  trailingSlash: true,

  images: {
    unoptimized: true,
  },
}

export default nextConfig